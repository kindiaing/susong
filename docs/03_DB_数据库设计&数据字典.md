# 03_DB 数据库设计 & 数据字典

对应 PRD 版本：V1.0  
对应 FSD 版本：V1.0  
技术栈：Laravel 13 + PHP 8.3 + MySQL 8.0 + Redis 7.x（缓存/队列/会话）

---

## 1 文档说明

本文档基于《01_PRD_业务需求.md》与《02_FSD_功能详细说明.md》编制，定义本地速送服务平台（含生鲜配送）的全量数据库结构、数据字典及 Migration 初始化脚本。  
文档包含两部分：

1. **常规表格形式**：每张表的中文目录、字段说明、约束与备注；
2. **Migration 格式**：可直接用于 Laravel 项目的 PHP Migration 代码（含中文注释），并填充默认数据。

---

## 2 数据库设计概述

- 数据库类型：MySQL 8.0
- 默认字符集：`utf8mb4`
- 默认排序规则：`utf8mb4_unicode_ci`
- 主键：统一使用 `bigint unsigned` 自增 ID，名称为 `id`
- 时间戳：统一使用 `created_at` / `updated_at`，删除标记使用 `deleted_at`（软删除）
- 金额字段：统一使用 `decimal(15, 2)` 或 `decimal(15, 4)`（单价/成本）
- 状态字段：使用 `tinyint` 或 `varchar` 枚举，默认值为 0 或业务初始状态
- 多租户数据隔离：商家相关表含 `merchant_id` 字段；司机/拣货员相关表含 `user_id` 或 `driver_id` 字段

---

## 3 命名规范

| 项目 | 规范 |
| :--- | :--- |
| 表名 | 小写、复数、下划线分隔，如 `purchase_orders` |
| 字段名 | 小写、下划线分隔，如 `settlement_status` |
| 外键字段 | 关联表名单数 + `_id`，如 `merchant_id` |
| 索引 | 前缀 `idx_` + 字段名，如 `idx_merchant_id` |
| 唯一索引 | 前缀 `uk_` + 字段名，如 `uk_phone` |
| Migration | 时间戳 + 动作 + 表名，如 `2026_01_01_000001_create_users_table` |
| 中文目录 | 表头备注、字段注释、目录名使用中文 |

---

## 4 模块与表清单

| 模块 | 中文目录 | 表名 | 说明 |
| :--- | :--- | :--- | :--- |
| 用户与权限 | 用户与权限 | `users` | 系统用户表 |
| 用户与权限 | 用户与权限 | `roles` | 角色表 |
| 用户与权限 | 用户与权限 | `permissions` | 权限表 |
| 用户与权限 | 用户与权限 | `model_has_roles` | 用户角色关联表 |
| 用户与权限 | 用户与权限 | `model_has_permissions` | 用户权限关联表 |
| 用户与权限 | 用户与权限 | `role_has_permissions` | 角色权限关联表 |
| 用户与权限 | 用户与权限 | `password_reset_tokens` | 密码重置令牌 |
| 用户与权限 | 用户与权限 | `personal_access_tokens` | Sanctum Token |
| 组织主体 | 供应商 | `suppliers` | 供应商 |
| 组织主体 | 商家 | `merchants` | 商家 |
| 组织主体 | 线路 | `delivery_routes` | 配送线路 |
| 组织主体 | 司机 | `drivers` | 司机 |
| 组织主体 | 车辆 | `vehicles` | 车辆 |
| 组织主体 | 司机车辆 | `driver_vehicles` | 司机车辆绑定 |
| 商品管理 | 分类 | `categories` | 商品分类 |
| 商品管理 | 商品 | `products` | 商品 |
| 商品管理 | 商品 | `product_images` | 商品图片 |
| 商品管理 | SKU | `skus` | SKU 规格 |
| 商品管理 | 可见性 | `merchant_sku_visibility` | 商家 SKU 可见性 |
| 商品管理 | 标签 | `tags` | 标签词库 |
| 商品管理 | 标签 | `product_tags` | 商品标签关联 |
| 商品管理 | 关键词 | `keywords` | 搜索关键词联想 |
| 平台统采 | 待采清单 | `purchase_items` | 待采清单 |
| 平台统采 | 采购单 | `purchase_orders` | 采购单 |
| 平台统采 | 采购单明细 | `purchase_order_items` | 采购单明细 |
| 客户直采 | 购物车 | `carts` | 购物车 |
| 客户直采 | 购物车 | `cart_items` | 购物车明细 |
| 客户直采 | 订单 | `orders` | 订单 |
| 客户直采 | 订单 | `order_items` | 订单明细 |
| 客户直采 | 常购 | `frequently_bought` | 常购清单 |
| 客户直采 | 复购 | `repurchase_templates` | 复购模板 |
| 客户直采 | 复购 | `repurchase_template_items` | 复购模板明细 |
| 库存管理 | 仓库 | `warehouses` | 仓库 |
| 库存管理 | 库存 | `inventory` | 实时库存 |
| 库存管理 | 库存 | `inventory_logs` | 库存变动日志 |
| 拣货管理 | 拣货 | `picking_tasks` | 拣货任务 |
| 拣货管理 | 拣货 | `picking_task_items` | 拣货任务明细 |
| 物流配送 | 配送 | `delivery_tasks` | 配送任务 |
| 物流配送 | 配送 | `delivery_task_orders` | 配送任务订单关联 |
| 物流配送 | 轨迹 | `delivery_tracks` | 配送轨迹 |
| 物流配送 | 签收 | `signatures` | 签收存证 |
| 物流配送 | 温度 | `temperatures` | 冷链温度记录 |
| 差异处理 | 差异 | `discrepancies` | 差异单 |
| 财务对账 | 账户 | `merchant_accounts` | 商家账户 |
| 财务对账 | 充值 | `recharges` | 充值记录 |
| 财务对账 | 供应商结算 | `supplier_settlements` | 供应商结算单 |
| 财务对账 | 供应商结算 | `supplier_settlement_items` | 结算单明细 |
| 财务对账 | 应收 | `receivables` | 应收账款 |
| 财务对账 | 发票 | `invoices` | 发票 |
| 财务对账 | 授权 | `correction_authorizations` | 单据授权更正 |
| 系统支撑 | 配置 | `system_configs` | 系统配置 |
| 系统支撑 | 广告 | `banners` | 轮播广告 |
| 系统支撑 | 主推 | `promotions` | 运营主推 |
| 系统支撑 | 日志 | `operation_logs` | 操作日志 |
| 系统支撑 | 日志 | `audit_logs` | 审计日志 |
| 微信 | 微信 | `wechat_users` | 微信用户绑定 |

---

## 5 表结构详细设计

### 5.1 用户与权限

#### 5.1.1 users（用户表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| username | varchar(50) | 否 | - | 用户名，唯一 |
| password | varchar(255) | 否 | - | bcrypt 加密密码 |
| name | varchar(50) | 否 | - | 姓名 |
| phone | varchar(20) | 是 | NULL | 手机号，唯一 |
| email | varchar(100) | 是 | NULL | 邮箱，唯一 |
| avatar | varchar(255) | 是 | NULL | 头像 |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| last_login_at | timestamp | 是 | NULL | 最后登录时间 |
| email_verified_at | timestamp | 是 | NULL | 邮箱验证时间 |
| remember_token | varchar(100) | 是 | NULL | Laravel 记住我令牌 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`uk_username`（username）、`uk_phone`（phone）、`uk_email`（email）
- 备注：超级管理员、运营、财务、拣货员、司机等内部员工统一存放于此。商家、司机可能有独立资料表并通过 user_id 关联。

#### 5.1.2 roles（角色表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| name | varchar(50) | 否 | - | 角色标识（英文） |
| guard_name | varchar(50) | 否 | web | 守卫名称 |
| display_name | varchar(50) | 否 | - | 角色显示名称（中文） |
| description | varchar(255) | 是 | NULL | 描述 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_name_guard`（name, guard_name）

#### 5.1.3 permissions（权限表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| name | varchar(50) | 否 | - | 权限标识（英文） |
| guard_name | varchar(50) | 否 | web | 守卫名称 |
| display_name | varchar(50) | 否 | - | 权限显示名称（中文） |
| type | tinyint unsigned | 否 | 1 | 类型：1 菜单，2 按钮，3 接口 |
| parent_id | bigint unsigned | 是 | 0 | 父级权限 ID |
| route | varchar(100) | 是 | NULL | 路由/接口标识 |
| sort | int unsigned | 否 | 0 | 排序 |
| icon | varchar(50) | 是 | NULL | 菜单图标 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_parent_id`（parent_id）、`uk_name_guard`（name, guard_name）

#### 5.1.4 model_has_roles（用户角色关联表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| role_id | bigint unsigned | 否 | - | 角色 ID |
| model_type | varchar(255) | 否 | - | 模型类型 |
| model_id | bigint unsigned | 否 | - | 模型 ID |

- 主键：`role_id`, `model_id`, `model_type`
- 索引：`idx_model`（model_id, model_type）

#### 5.1.5 model_has_permissions（用户权限关联表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| permission_id | bigint unsigned | 否 | - | 权限 ID |
| model_type | varchar(255) | 否 | - | 模型类型 |
| model_id | bigint unsigned | 否 | - | 模型 ID |

- 主键：`permission_id`, `model_id`, `model_type`
- 索引：`idx_model`（model_id, model_type）

#### 5.1.6 role_has_permissions（角色权限关联表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| permission_id | bigint unsigned | 否 | - | 权限 ID |
| role_id | bigint unsigned | 否 | - | 角色 ID |

- 主键：`permission_id`, `role_id`

#### 5.1.7 password_reset_tokens（密码重置令牌）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| email | varchar(255) | 否 | - | 邮箱 |
| token | varchar(255) | 否 | - | 令牌 |
| created_at | timestamp | 是 | NULL | 创建时间 |

- 主键：`email`

#### 5.1.8 personal_access_tokens（Sanctum Token 表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| tokenable_type | varchar(255) | 否 | - | 模型类型 |
| tokenable_id | bigint unsigned | 否 | - | 模型 ID |
| name | varchar(255) | 否 | - | Token 名称 |
| token | varchar(64) | 否 | - | Token 值 |
| abilities | text | 是 | NULL | 能力列表 |
| last_used_at | timestamp | 是 | NULL | 最后使用时间 |
| expires_at | timestamp | 是 | NULL | 过期时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_tokenable`（tokenable_id, tokenable_type）、`uk_token`（token）



### 5.2 组织主体

#### 5.2.1 suppliers（供应商表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| name | varchar(100) | 否 | - | 供应商名称 |
| contact_name | varchar(50) | 是 | NULL | 联系人 |
| contact_phone | varchar(20) | 是 | NULL | 联系电话 |
| address | varchar(255) | 是 | NULL | 地址 |
| bank_name | varchar(100) | 是 | NULL | 开户行 |
| bank_account | varchar(50) | 是 | NULL | 银行账号 |
| settlement_cycle | tinyint unsigned | 否 | 1 | 结算周期：1 周结，2 月结 |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| remark | text | 是 | NULL | 备注 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`idx_status`（status）

#### 5.2.2 merchants（商家表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| user_id | bigint unsigned | 是 | NULL | 关联登录用户 ID |
| name | varchar(100) | 否 | - | 商家名称 |
| contact_name | varchar(50) | 是 | NULL | 联系人 |
| contact_phone | varchar(20) | 是 | NULL | 联系电话 |
| address | varchar(255) | 是 | NULL | 默认配送地址 |
| delivery_route_id | bigint unsigned | 是 | NULL | 所属配送线路 ID |
| delivery_sort | int unsigned | 否 | 0 | 配送顺序 |
| min_order_amount | decimal(15, 2) | 否 | 0.00 | 起送价 |
| settlement_type | tinyint unsigned | 否 | 1 | 结算方式：1 现结，2 账期，3 预付款 |
| credit_limit | decimal(15, 2) | 否 | 0.00 | 信用额度 |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| remark | text | 是 | NULL | 备注 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`idx_user_id`（user_id）、`idx_delivery_route_id`（delivery_route_id）、`idx_status`（status）

#### 5.2.3 delivery_routes（配送线路表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| name | varchar(50) | 否 | - | 线路名称 |
| description | varchar(255) | 是 | NULL | 描述 |
| sort | int unsigned | 否 | 0 | 排序 |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`idx_status`（status）

#### 5.2.4 drivers（司机表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| user_id | bigint unsigned | 是 | NULL | 关联登录用户 ID |
| name | varchar(50) | 否 | - | 姓名 |
| phone | varchar(20) | 否 | - | 手机号，唯一 |
| id_card | varchar(18) | 是 | NULL | 身份证号 |
| online_status | tinyint unsigned | 否 | 0 | 在线状态：0 离线，1 在线 |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`uk_phone`（phone）、`idx_user_id`（user_id）、`idx_status`（status）

#### 5.2.5 vehicles（车辆表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| plate_number | varchar(20) | 否 | - | 车牌号，唯一 |
| vehicle_type | varchar(50) | 是 | NULL | 车辆类型 |
| is_cold_chain | tinyint unsigned | 否 | 0 | 是否冷链：0 否，1 是 |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`uk_plate_number`（plate_number）、`idx_status`（status）

#### 5.2.6 driver_vehicles（司机车辆绑定表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| driver_id | bigint unsigned | 否 | - | 司机 ID |
| vehicle_id | bigint unsigned | 否 | - | 车辆 ID |
| is_default | tinyint unsigned | 否 | 0 | 是否默认车辆：0 否，1 是 |
| bound_at | timestamp | 是 | NULL | 绑定时间 |
| unbound_at | timestamp | 是 | NULL | 解绑时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_driver_id`（driver_id）、`idx_vehicle_id`（vehicle_id）

---

### 5.3 商品管理

#### 5.3.1 categories（商品分类表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| parent_id | bigint unsigned | 否 | 0 | 父级分类 ID，0 为根节点 |
| name | varchar(50) | 否 | - | 分类名称 |
| icon | varchar(255) | 是 | NULL | 图标 |
| sort | int unsigned | 否 | 0 | 排序 |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`idx_parent_id`（parent_id）、`idx_status`（status）

#### 5.3.2 products（商品表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| category_id | bigint unsigned | 否 | - | 分类 ID |
| supplier_id | bigint unsigned | 是 | NULL | 默认供应商 ID |
| name | varchar(100) | 否 | - | 商品名称 |
| cover | varchar(255) | 是 | NULL | 封面图 |
| unit | varchar(20) | 否 | - | 单位：斤/箱/份等 |
| is_weight_priced | tinyint unsigned | 否 | 0 | 是否称重改价：0 否，1 是 |
| stock_warning_value | decimal(15, 3) | 否 | 0.000 | 库存预警值 |
| status | tinyint unsigned | 否 | 1 | 状态：0 下架，1 上架 |
| description | text | 是 | NULL | 商品详情 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`idx_category_id`（category_id）、`idx_supplier_id`（supplier_id）、`idx_status`（status）

#### 5.3.3 product_images（商品图片表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| product_id | bigint unsigned | 否 | - | 商品 ID |
| image_url | varchar(255) | 否 | - | 图片地址 |
| sort | int unsigned | 否 | 0 | 排序 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_product_id`（product_id）

#### 5.3.4 skus（SKU 规格表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| product_id | bigint unsigned | 否 | - | 商品 ID |
| sku_code | varchar(50) | 否 | - | SKU 编码，唯一 |
| specs | json | 是 | NULL | 规格属性（如 {\"规格\": \"大份\"}） |
| purchase_price | decimal(15, 4) | 否 | 0.0000 | 采购参考价 |
| wholesale_price | decimal(15, 4) | 否 | 0.0000 | 批发销售价 |
| cost_price | decimal(15, 4) | 否 | 0.0000 | 财务成本价 |
| stock | decimal(15, 3) | 否 | 0.000 | 当前库存（冗余字段） |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`idx_product_id`（product_id）、`uk_sku_code`（sku_code）、`idx_status`（status）

#### 5.3.5 merchant_sku_visibility（商家 SKU 可见性表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| merchant_id | bigint unsigned | 否 | - | 商家 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| is_visible | tinyint unsigned | 否 | 1 | 是否可见：0 否，1 是 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_merchant_sku`（merchant_id, sku_id）、`idx_sku_id`（sku_id）

#### 5.3.6 tags（标签词库表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| name | varchar(50) | 否 | - | 标签名称，唯一 |
| sort | int unsigned | 否 | 0 | 排序 |
| status | tinyint unsigned | 否 | 1 | 状态 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_name`（name）

#### 5.3.7 product_tags（商品标签关联表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| product_id | bigint unsigned | 否 | - | 商品 ID |
| tag_id | bigint unsigned | 否 | - | 标签 ID |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_product_tag`（product_id, tag_id）、`idx_tag_id`（tag_id）

#### 5.3.8 keywords（搜索关键词表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| keyword | varchar(50) | 否 | - | 关键词 |
| product_id | bigint unsigned | 是 | NULL | 关联商品 ID |
| search_count | int unsigned | 否 | 0 | 搜索次数 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_product_id`（product_id）、`idx_keyword`（keyword）



---

### 5.4 平台统采

#### 5.4.1 purchase_items（待采清单表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| quantity | decimal(15, 3) | 否 | 0.000 | 待采数量 |
| source_type | tinyint unsigned | 否 | 1 | 来源：1 订单汇总，2 手工添加 |
| source_id | bigint unsigned | 是 | NULL | 来源业务 ID |
| status | tinyint unsigned | 否 | 1 | 状态：1 待生成采购单，2 已生成采购单 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_sku_id`（sku_id）、`idx_status`（status）

#### 5.4.2 purchase_orders（采购单表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| order_no | varchar(50) | 否 | - | 采购单号，唯一 |
| supplier_id | bigint unsigned | 否 | - | 供应商 ID |
| status | tinyint unsigned | 否 | 1 | 状态：1 待接单，2 备货中，3 已发货，4 已入库，5 完成，9 取消 |
| total_amount | decimal(15, 2) | 否 | 0.00 | 总金额 |
| actual_amount | decimal(15, 2) | 否 | 0.00 | 实际入库金额 |
| remark | text | 是 | NULL | 备注 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`uk_order_no`（order_no）、`idx_supplier_id`（supplier_id）、`idx_status`（status）

#### 5.4.3 purchase_order_items（采购单明细表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| purchase_order_id | bigint unsigned | 否 | - | 采购单 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| quantity | decimal(15, 3) | 否 | 0.000 | 采购数量 |
| price | decimal(15, 4) | 否 | 0.0000 | 采购单价 |
| actual_quantity | decimal(15, 3) | 否 | 0.000 | 实际入库数量 |
| actual_price | decimal(15, 4) | 否 | 0.0000 | 实际入库单价 |
| amount | decimal(15, 2) | 否 | 0.00 | 金额 |
| actual_amount | decimal(15, 2) | 否 | 0.00 | 实际金额 |
| discrepancy_reason | varchar(255) | 是 | NULL | 入库差异原因 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_purchase_order_id`（purchase_order_id）、`idx_sku_id`（sku_id）

---

### 5.5 客户直采

#### 5.5.1 carts（购物车表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| merchant_id | bigint unsigned | 否 | - | 商家 ID |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_merchant_id`（merchant_id）

#### 5.5.2 cart_items（购物车明细表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| cart_id | bigint unsigned | 否 | - | 购物车 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| quantity | decimal(15, 3) | 否 | 0.000 | 数量 |
| price | decimal(15, 4) | 否 | 0.0000 | 加入时单价 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_cart_id`（cart_id）、`idx_sku_id`（sku_id）

#### 5.5.3 orders（订单表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| order_no | varchar(50) | 否 | - | 订单号，唯一 |
| merchant_id | bigint unsigned | 否 | - | 商家 ID |
| delivery_route_id | bigint unsigned | 是 | NULL | 配送线路 ID |
| batch | tinyint unsigned | 否 | 1 | 配送批次：1 上午，2 下午 |
| delivery_address | varchar(255) | 是 | NULL | 配送地址 |
| contact_name | varchar(50) | 是 | NULL | 收货联系人 |
| contact_phone | varchar(20) | 是 | NULL | 收货电话 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待拣货，2 拣货中，3 配送中，4 已签收，5 已锁定，9 已取消 |
| total_amount | decimal(15, 2) | 否 | 0.00 | 原始订单金额 |
| adjusted_amount | decimal(15, 2) | 否 | 0.00 | 调整后金额 |
| final_amount | decimal(15, 2) | 否 | 0.00 | 最终结算金额 |
| payment_status | tinyint unsigned | 否 | 1 | 支付状态：1 未支付，2 已支付，3 账期 |
| settlement_type | tinyint unsigned | 否 | 1 | 结算方式：1 现结，2 账期，3 预付款 |
| is_locked | tinyint unsigned | 否 | 0 | 是否锁定：0 否，1 是 |
| remark | text | 是 | NULL | 备注 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`uk_order_no`（order_no）、`idx_merchant_id`（merchant_id）、`idx_status`（status）、`idx_batch`（batch）

#### 5.5.4 order_items（订单明细表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| order_id | bigint unsigned | 否 | - | 订单 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| product_name | varchar(100) | 否 | - | 商品名称（快照） |
| sku_specs | json | 是 | NULL | 规格快照 |
| quantity | decimal(15, 3) | 否 | 0.000 | 下单数量 |
| price | decimal(15, 4) | 否 | 0.0000 | 下单单价 |
| actual_quantity | decimal(15, 3) | 否 | 0.000 | 实际称重数量 |
| actual_price | decimal(15, 4) | 否 | 0.0000 | 实际称重单价 |
| subtotal | decimal(15, 2) | 否 | 0.00 | 小计金额 |
| actual_subtotal | decimal(15, 2) | 否 | 0.00 | 实际小计金额 |
| discrepancy_amount | decimal(15, 2) | 否 | 0.00 | 差异金额 |
| status | tinyint unsigned | 否 | 1 | 状态：1 正常，2 待审核，3 已调整 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_order_id`（order_id）、`idx_sku_id`（sku_id）

#### 5.5.5 frequently_bought（常购清单表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| merchant_id | bigint unsigned | 否 | - | 商家 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| buy_count | int unsigned | 否 | 0 | 购买次数 |
| last_buy_at | timestamp | 是 | NULL | 最近购买时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_merchant_sku`（merchant_id, sku_id）、`idx_sku_id`（sku_id）

#### 5.5.6 repurchase_templates（复购模板表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| merchant_id | bigint unsigned | 否 | - | 商家 ID |
| name | varchar(50) | 否 | - | 模板名称 |
| status | tinyint unsigned | 否 | 1 | 状态 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_merchant_id`（merchant_id）

#### 5.5.7 repurchase_template_items（复购模板明细表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| template_id | bigint unsigned | 否 | - | 模板 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| quantity | decimal(15, 3) | 否 | 0.000 | 数量 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_template_id`（template_id）、`idx_sku_id`（sku_id）

---

### 5.6 库存管理

#### 5.6.1 warehouses（仓库表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| name | varchar(50) | 否 | - | 仓库名称 |
| type | tinyint unsigned | 否 | 1 | 类型：1 总仓，2 前置仓 |
| is_cold_chain | tinyint unsigned | 否 | 0 | 是否冷链：0 否，1 是 |
| address | varchar(255) | 是 | NULL | 地址 |
| status | tinyint unsigned | 否 | 1 | 状态 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`idx_status`（status）

#### 5.6.2 inventory（实时库存表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| warehouse_id | bigint unsigned | 否 | - | 仓库 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| total_stock | decimal(15, 3) | 否 | 0.000 | 总库存 |
| locked_stock | decimal(15, 3) | 否 | 0.000 | 锁定库存 |
| available_stock | decimal(15, 3) | 否 | 0.000 | 可用库存 |
| batch_no | varchar(50) | 是 | NULL | 入库批次号 |
| expiry_date | date | 是 | NULL | 效期 |
| warning_value | decimal(15, 3) | 否 | 0.000 | 预警值 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_warehouse_sku_batch`（warehouse_id, sku_id, batch_no）、`idx_sku_id`（sku_id）

#### 5.6.3 inventory_logs（库存变动日志表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| warehouse_id | bigint unsigned | 否 | - | 仓库 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| type | tinyint unsigned | 否 | - | 变动类型：1 入库，2 出库，3 调拨，4 报损，5 报溢，6 调整 |
| quantity | decimal(15, 3) | 否 | 0.000 | 变动数量（正增负减） |
| before_stock | decimal(15, 3) | 否 | 0.000 | 变动前库存 |
| after_stock | decimal(15, 3) | 否 | 0.000 | 变动后库存 |
| reason | varchar(255) | 是 | NULL | 变动原因 |
| operator_id | bigint unsigned | 是 | NULL | 操作人 ID |
| source_type | varchar(50) | 是 | NULL | 业务来源类型 |
| source_id | bigint unsigned | 是 | NULL | 业务来源 ID |
| created_at | timestamp | 是 | NULL | 创建时间 |

- 索引：`idx_warehouse_id`（warehouse_id）、`idx_sku_id`（sku_id）、`idx_type`（type）、`idx_created_at`（created_at）

---

### 5.7 拣货管理

#### 5.7.1 picking_tasks（拣货任务表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| task_no | varchar(50) | 否 | - | 任务编号，唯一 |
| warehouse_id | bigint unsigned | 否 | - | 仓库 ID |
| picker_id | bigint unsigned | 是 | NULL | 拣货员 ID |
| batch | tinyint unsigned | 否 | 1 | 配送批次：1 上午，2 下午 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待分配，2 拣货中，3 已完成 |
| started_at | timestamp | 是 | NULL | 开始时间 |
| completed_at | timestamp | 是 | NULL | 完成时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_task_no`（task_no）、`idx_warehouse_id`（warehouse_id）、`idx_picker_id`（picker_id）、`idx_status`（status）

#### 5.7.2 picking_task_items（拣货任务明细表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| picking_task_id | bigint unsigned | 否 | - | 拣货任务 ID |
| order_id | bigint unsigned | 否 | - | 订单 ID |
| order_item_id | bigint unsigned | 否 | - | 订单明细 ID |
| sku_id | bigint unsigned | 否 | - | SKU ID |
| required_quantity | decimal(15, 3) | 否 | 0.000 | 需求数量 |
| picked_quantity | decimal(15, 3) | 否 | 0.000 | 实际拣货数量 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待拣货，2 已拣货，3 差异 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_picking_task_id`（picking_task_id）、`idx_order_id`（order_id）、`idx_sku_id`（sku_id）

---

### 5.8 物流配送

#### 5.8.1 delivery_tasks（配送任务表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| task_no | varchar(50) | 否 | - | 任务编号，唯一 |
| delivery_route_id | bigint unsigned | 否 | - | 线路 ID |
| driver_id | bigint unsigned | 是 | NULL | 司机 ID |
| vehicle_id | bigint unsigned | 是 | NULL | 车辆 ID |
| batch | tinyint unsigned | 否 | 1 | 配送批次：1 上午，2 下午 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待配送，2 配送中，3 任务完成 |
| planned_at | timestamp | 是 | NULL | 计划配送时间 |
| started_at | timestamp | 是 | NULL | 开始时间 |
| completed_at | timestamp | 是 | NULL | 完成时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_task_no`（task_no）、`idx_route_id`（delivery_route_id）、`idx_driver_id`（driver_id）、`idx_status`（status）

#### 5.8.2 delivery_task_orders（配送任务订单关联表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| delivery_task_id | bigint unsigned | 否 | - | 配送任务 ID |
| order_id | bigint unsigned | 否 | - | 订单 ID |
| delivery_sort | int unsigned | 否 | 0 | 配送顺序 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待配送，2 已送达 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_delivery_task_id`（delivery_task_id）、`idx_order_id`（order_id）

#### 5.8.3 delivery_tracks（配送轨迹表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| delivery_task_id | bigint unsigned | 否 | - | 配送任务 ID |
| driver_id | bigint unsigned | 否 | - | 司机 ID |
| latitude | decimal(10, 7) | 否 | 0.0000000 | 纬度 |
| longitude | decimal(10, 7) | 否 | 0.0000000 | 经度 |
| location_desc | varchar(255) | 是 | NULL | 位置描述 |
| reported_at | timestamp | 否 | NULL | 上报时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |

- 索引：`idx_delivery_task_id`（delivery_task_id）、`idx_driver_id`（driver_id）、`idx_reported_at`（reported_at）

#### 5.8.4 signatures（签收存证表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| order_id | bigint unsigned | 否 | - | 订单 ID |
| delivery_task_id | bigint unsigned | 否 | - | 配送任务 ID |
| type | tinyint unsigned | 否 | 1 | 类型：1 拍照签收，2 电子签名，3 质检照片 |
| image_url | varchar(255) | 是 | NULL | 图片/签名文件地址 |
| signer_name | varchar(50) | 是 | NULL | 签收人 |
| signed_at | timestamp | 是 | NULL | 签收时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_order_id`（order_id）、`idx_delivery_task_id`（delivery_task_id）、`idx_type`（type）

#### 5.8.5 temperatures（冷链温度记录表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| delivery_task_id | bigint unsigned | 否 | - | 配送任务 ID |
| temperature | decimal(5, 2) | 否 | 0.00 | 温度值 |
| recorded_at | timestamp | 否 | NULL | 记录时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |

- 索引：`idx_delivery_task_id`（delivery_task_id）、`idx_recorded_at`（recorded_at）

---

### 5.9 差异处理

#### 5.9.1 discrepancies（差异单表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| discrepancy_no | varchar(50) | 否 | - | 差异单号，唯一 |
| order_id | bigint unsigned | 否 | - | 关联订单 ID |
| order_item_id | bigint unsigned | 是 | NULL | 关联订单明细 ID |
| stage | tinyint unsigned | 否 | - | 差异环节：1 拣货，2 配送，3 实收 |
| type | tinyint unsigned | 否 | 1 | 差异类型：1 少收，2 拒收，3 残次，4 其他 |
| expected_quantity | decimal(15, 3) | 否 | 0.000 | 预期数量 |
| actual_quantity | decimal(15, 3) | 否 | 0.000 | 实际数量 |
| quantity_diff | decimal(15, 3) | 否 | 0.000 | 数量差异 |
| amount_diff | decimal(15, 2) | 否 | 0.00 | 金额差异 |
| reason | varchar(255) | 是 | NULL | 差异原因 |
| evidence_urls | json | 是 | NULL | 凭证图片数组 |
| responsible_party | tinyint unsigned | 是 | NULL | 责任方：1 供应商，2 平台，3 司机，4 商家 |
| decision | tinyint unsigned | 是 | NULL | 处理决策：1 补货，2 退款，3 扣款，4 报损，5 不计 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待处理，2 处理中，3 已处理，4 已关闭 |
| handler_id | bigint unsigned | 是 | NULL | 处理人 ID |
| handled_at | timestamp | 是 | NULL | 处理时间 |
| is_amount_adjusted | tinyint unsigned | 否 | 0 | 是否已调整金额 |
| remark | text | 是 | NULL | 备注 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`uk_discrepancy_no`（discrepancy_no）、`idx_order_id`（order_id）、`idx_order_item_id`（order_item_id）、`idx_stage`（stage）、`idx_status`（status）

---

### 5.10 财务对账

#### 5.10.1 merchant_accounts（商家账户表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| merchant_id | bigint unsigned | 否 | - | 商家 ID |
| balance | decimal(15, 2) | 否 | 0.00 | 账户余额 |
| total_recharge | decimal(15, 2) | 否 | 0.00 | 总充值 |
| total_consumption | decimal(15, 2) | 否 | 0.00 | 总消费 |
| credit_limit | decimal(15, 2) | 否 | 0.00 | 信用额度 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |

- 索引：`uk_merchant_id`（merchant_id）

#### 5.10.2 recharges（充值记录表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| merchant_id | bigint unsigned | 否 | - | 商家 ID |
| amount | decimal(15, 2) | 否 | 0.00 | 充值金额 |
| payment_method | tinyint unsigned | 否 | 1 | 支付方式：1 微信支付，2 线下转账，3 后台手工 |
| transaction_no | varchar(100) | 是 | NULL | 第三方交易号 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待确认，2 成功，3 失败 |
| operator_id | bigint unsigned | 是 | NULL | 操作人 ID |
| remark | varchar(255) | 是 | NULL | 备注 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_merchant_id`（merchant_id）、`idx_transaction_no`（transaction_no）、`idx_status`（status）

#### 5.10.3 supplier_settlements（供应商结算单表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| settlement_no | varchar(50) | 否 | - | 结算单号，唯一 |
| supplier_id | bigint unsigned | 否 | - | 供应商 ID |
| start_date | date | 否 | - | 结算周期开始 |
| end_date | date | 否 | - | 结算周期结束 |
| total_amount | decimal(15, 2) | 否 | 0.00 | 汇总金额 |
| service_fee | decimal(15, 2) | 否 | 0.00 | 服务费 |
| payable_amount | decimal(15, 2) | 否 | 0.00 | 应付金额 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待结算，2 已结算 |
| settled_at | timestamp | 是 | NULL | 结算时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_settlement_no`（settlement_no）、`idx_supplier_id`（supplier_id）、`idx_status`（status）

#### 5.10.4 supplier_settlement_items（结算单明细表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| supplier_settlement_id | bigint unsigned | 否 | - | 结算单 ID |
| purchase_order_id | bigint unsigned | 否 | - | 采购单 ID |
| amount | decimal(15, 2) | 否 | 0.00 | 金额 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_supplier_settlement_id`（supplier_settlement_id）、`idx_purchase_order_id`（purchase_order_id）

#### 5.10.5 receivables（应收账款表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| receivable_no | varchar(50) | 否 | - | 应收单号，唯一 |
| order_id | bigint unsigned | 否 | - | 订单 ID |
| merchant_id | bigint unsigned | 否 | - | 商家 ID |
| original_amount | decimal(15, 2) | 否 | 0.00 | 原始金额 |
| adjusted_amount | decimal(15, 2) | 否 | 0.00 | 调整后金额 |
| discrepancy_amount | decimal(15, 2) | 否 | 0.00 | 差异金额 |
| paid_amount | decimal(15, 2) | 否 | 0.00 | 已付金额 |
| status | tinyint unsigned | 否 | 1 | 状态：1 未结算，2 已结算，3 争议中 |
| settlement_type | tinyint unsigned | 否 | 1 | 结算方式：1 现结，2 账期，3 预付款 |
| due_date | date | 是 | NULL | 到期日 |
| settled_at | timestamp | 是 | NULL | 结算时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_receivable_no`（receivable_no）、`idx_order_id`（order_id）、`idx_merchant_id`（merchant_id）、`idx_status`（status）

#### 5.10.6 invoices（发票表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| invoice_no | varchar(50) | 是 | NULL | 发票号 |
| type | tinyint unsigned | 否 | 1 | 类型：1 客户发票，2 供应商发票 |
| target_id | bigint unsigned | 否 | - | 关联对象 ID（merchant_id 或 supplier_id） |
| title | varchar(100) | 是 | NULL | 发票抬头 |
| amount | decimal(15, 2) | 否 | 0.00 | 金额 |
| file_url | varchar(255) | 是 | NULL | 发票文件地址 |
| status | tinyint unsigned | 否 | 1 | 状态：1 待开具，2 已开具，3 已寄出 |
| applied_at | timestamp | 是 | NULL | 申请时间 |
| issued_at | timestamp | 是 | NULL | 开具时间 |
| sent_at | timestamp | 是 | NULL | 寄出时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_target_id`（target_id）、`idx_type`（type）、`idx_status`（status）

#### 5.10.7 correction_authorizations（单据授权更正表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| order_id | bigint unsigned | 否 | - | 订单 ID |
| operator_id | bigint unsigned | 否 | - | 授权人 ID |
| reason | varchar(255) | 否 | - | 更正原因 |
| before_data | json | 是 | NULL | 修改前数据 |
| after_data | json | 是 | NULL | 修改后数据 |
| authorized_at | timestamp | 否 | NULL | 授权时间 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_order_id`（order_id）、`idx_operator_id`（operator_id）

---

### 5.11 系统支撑

#### 5.11.1 system_configs（系统配置表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| config_key | varchar(100) | 否 | - | 配置键，唯一 |
| config_value | text | 是 | NULL | 配置值 |
| description | varchar(255) | 是 | NULL | 说明 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_config_key`（config_key）

#### 5.11.2 banners（轮播广告表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| title | varchar(100) | 否 | - | 标题 |
| image_url | varchar(255) | 否 | - | 图片地址 |
| link_url | varchar(255) | 是 | NULL | 跳转链接 |
| sort | int unsigned | 否 | 0 | 排序 |
| status | tinyint unsigned | 否 | 1 | 状态：0 禁用，1 启用 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |
| deleted_at | timestamp | 是 | NULL | 软删除时间 |

- 索引：`idx_status`（status）、`idx_sort`（sort）

#### 5.11.3 promotions（运营主推表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| type | tinyint unsigned | 否 | 1 | 类型：1 主推商品，2 主推品类 |
| target_id | bigint unsigned | 否 | - | 目标 ID（sku_id 或 category_id） |
| sort | int unsigned | 否 | 0 | 排序 |
| start_at | timestamp | 是 | NULL | 开始时间 |
| end_at | timestamp | 是 | NULL | 结束时间 |
| status | tinyint unsigned | 否 | 1 | 状态 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`idx_type`（type）、`idx_target_id`（target_id）、`idx_status`（status）

#### 5.11.4 operation_logs（操作日志表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| user_id | bigint unsigned | 是 | NULL | 操作人 ID |
| username | varchar(50) | 是 | NULL | 操作人用户名 |
| method | varchar(10) | 是 | NULL | 请求方法 |
| path | varchar(255) | 是 | NULL | 请求路径 |
| ip | varchar(50) | 是 | NULL | IP 地址 |
| content | text | 是 | NULL | 操作内容 |
| created_at | timestamp | 是 | NULL | 创建时间 |

- 索引：`idx_user_id`（user_id）、`idx_created_at`（created_at）

#### 5.11.5 audit_logs（审计日志表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| model_type | varchar(100) | 否 | - | 模型类型 |
| model_id | bigint unsigned | 否 | - | 模型 ID |
| action | varchar(50) | 否 | - | 操作动作：create/update/delete/lock/correction |
| before_data | json | 是 | NULL | 修改前数据 |
| after_data | json | 是 | NULL | 修改后数据 |
| operator_id | bigint unsigned | 是 | NULL | 操作人 ID |
| reason | varchar(255) | 是 | NULL | 操作原因 |
| relation_type | varchar(50) | 是 | NULL | 关联类型（order/delivery_task） |
| relation_id | bigint unsigned | 是 | NULL | 关联 ID |
| created_at | timestamp | 是 | NULL | 创建时间 |

- 索引：`idx_model`（model_type, model_id）、`idx_operator_id`（operator_id）、`idx_created_at`（created_at）

---

### 5.12 微信

#### 5.12.1 wechat_users（微信用户绑定表）

| 字段名 | 数据类型 | 可空 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | bigint unsigned | 否 | 自增 | 主键 |
| user_id | bigint unsigned | 是 | NULL | 关联系统用户 ID（司机/商家） |
| openid | varchar(100) | 否 | - | 微信 OpenID，唯一 |
| unionid | varchar(100) | 是 | NULL | 微信 UnionID |
| nickname | varchar(50) | 是 | NULL | 昵称 |
| avatar | varchar(255) | 是 | NULL | 头像 |
| type | tinyint unsigned | 否 | 1 | 类型：1 商家端，2 司机端 |
| status | tinyint unsigned | 否 | 1 | 状态 |
| created_at | timestamp | 是 | NULL | 创建时间 |
| updated_at | timestamp | 是 | NULL | 更新时间 |

- 索引：`uk_openid`（openid）、`idx_user_id`（user_id）、`idx_type`（type）

---

## 6 数据库关系图（文字版）

```text
users
├── roles (via model_has_roles)
├── permissions (via model_has_permissions)
├── personal_access_tokens
└── drivers / merchants / wechat_users

suppliers
├── products
└── purchase_orders

merchants
├── delivery_routes
├── carts
├── orders
├── merchant_accounts
├── recharges
├── receivables
├── frequently_bought
├── repurchase_templates
└── merchant_sku_visibility

delivery_routes
├── merchants
└── delivery_tasks

drivers
├── driver_vehicles
└── delivery_tasks

vehicles
└── driver_vehicles

categories
└── products

products
├── skus
├── product_images
├── product_tags
└── keywords

skus
├── inventory
├── order_items
├── cart_items
├── purchase_order_items
├── purchase_items
├── merchant_sku_visibility
└── frequently_bought

orders
├── order_items
├── discrepancies
├── receivables
├── signatures
├── picking_task_items
└── delivery_task_orders

purchase_orders
├── purchase_order_items
└── supplier_settlement_items

warehouses
├── inventory
├── inventory_logs
└── picking_tasks

picking_tasks
└── picking_task_items

delivery_tasks
├── delivery_task_orders
├── delivery_tracks
├── signatures
└── temperatures

supplier_settlements
└── supplier_settlement_items
```



## 7 Migration 脚本（含中文注释）

> 以下 Migration 文件按 Laravel 风格编写，可直接放入 `database/migrations/` 目录执行 `php artisan migrate`。  
> 每张表均包含中文注释，`users` 表默认插入超级管理员账号：  
> - 用户名：`seeding`  
> - 邮箱：`seeding@ihopeso.cn`  
> - 手机号：`15690631151`  
> - 初始密码：`Password`（使用 `bcrypt('Password')` 加密）  

### 7.1 用户与权限模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Hash;

return new class extends Migration
{
    public function up(): void
    {
        // 用户表：系统管理员、运营、财务、拣货员、司机等内部账号
        Schema::create('users', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('username', 50)->unique()->comment('用户名');
            $table->string('password', 255)->comment('bcrypt加密密码');
            $table->string('name', 50)->comment('姓名');
            $table->string('phone', 20)->nullable()->unique()->comment('手机号');
            $table->string('email', 100)->nullable()->unique()->comment('邮箱');
            $table->string('avatar', 255)->nullable()->comment('头像');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0禁用，1启用');
            $table->timestamp('last_login_at')->nullable()->comment('最后登录时间');
            $table->timestamp('email_verified_at')->nullable()->comment('邮箱验证时间');
            $table->string('remember_token', 100)->nullable()->comment('记住我令牌');
            $table->timestamps();
            $table->softDeletes();

            $table->comment('用户表');
        });

        // 角色表
        Schema::create('roles', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('name', 50)->comment('角色标识');
            $table->string('guard_name', 50)->default('web')->comment('守卫名称');
            $table->string('display_name', 50)->comment('角色显示名称');
            $table->string('description', 255)->nullable()->comment('描述');
            $table->timestamps();

            $table->unique(['name', 'guard_name']);
            $table->comment('角色表');
        });

        // 权限表
        Schema::create('permissions', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('name', 50)->comment('权限标识');
            $table->string('guard_name', 50)->default('web')->comment('守卫名称');
            $table->string('display_name', 50)->comment('权限显示名称');
            $table->tinyInteger('type')->unsigned()->default(1)->comment('类型：1菜单，2按钮，3接口');
            $table->unsignedBigInteger('parent_id')->default(0)->comment('父级权限ID');
            $table->string('route', 100)->nullable()->comment('路由/接口标识');
            $table->unsignedInteger('sort')->default(0)->comment('排序');
            $table->string('icon', 50)->nullable()->comment('菜单图标');
            $table->timestamps();

            $table->index('parent_id');
            $table->unique(['name', 'guard_name']);
            $table->comment('权限表');
        });

        // 用户角色关联表
        Schema::create('model_has_roles', function (Blueprint $table) {
            $table->unsignedBigInteger('role_id')->comment('角色ID');
            $table->string('model_type', 255)->comment('模型类型');
            $table->unsignedBigInteger('model_id')->comment('模型ID');

            $table->primary(['role_id', 'model_id', 'model_type']);
            $table->index(['model_id', 'model_type'], 'idx_model');
            $table->comment('用户角色关联表');
        });

        // 用户权限关联表
        Schema::create('model_has_permissions', function (Blueprint $table) {
            $table->unsignedBigInteger('permission_id')->comment('权限ID');
            $table->string('model_type', 255)->comment('模型类型');
            $table->unsignedBigInteger('model_id')->comment('模型ID');

            $table->primary(['permission_id', 'model_id', 'model_type']);
            $table->index(['model_id', 'model_type'], 'idx_model');
            $table->comment('用户权限关联表');
        });

        // 角色权限关联表
        Schema::create('role_has_permissions', function (Blueprint $table) {
            $table->unsignedBigInteger('permission_id')->comment('权限ID');
            $table->unsignedBigInteger('role_id')->comment('角色ID');

            $table->primary(['permission_id', 'role_id']);
            $table->comment('角色权限关联表');
        });

        // 密码重置令牌
        Schema::create('password_reset_tokens', function (Blueprint $table) {
            $table->string('email', 255)->primary()->comment('邮箱');
            $table->string('token', 255)->comment('令牌');
            $table->timestamp('created_at')->nullable()->comment('创建时间');
            $table->comment('密码重置令牌');
        });

        // Sanctum Token 表
        Schema::create('personal_access_tokens', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('tokenable_type', 255)->comment('模型类型');
            $table->unsignedBigInteger('tokenable_id')->comment('模型ID');
            $table->string('name', 255)->comment('Token名称');
            $table->string('token', 64)->unique()->comment('Token值');
            $table->text('abilities')->nullable()->comment('能力列表');
            $table->timestamp('last_used_at')->nullable()->comment('最后使用时间');
            $table->timestamp('expires_at')->nullable()->comment('过期时间');
            $table->timestamps();

            $table->index(['tokenable_id', 'tokenable_token']);
            $table->comment('Sanctum Token表');
        });

        // 初始化角色
        $roleIds = DB::table('roles')->insertGetId([
            ['name' => 'super_admin', 'guard_name' => 'web', 'display_name' => '超级管理员', 'description' => '全部功能、系统配置、账号管理', 'created_at' => now(), 'updated_at' => now()],
            ['name' => 'operator', 'guard_name' => 'web', 'display_name' => '运营管理员', 'description' => '商品、订单、商家、供应商管理', 'created_at' => now(), 'updated_at' => now()],
            ['name' => 'finance', 'guard_name' => 'web', 'display_name' => '财务人员', 'description' => '应收、结算、发票、审计', 'created_at' => now(), 'updated_at' => now()],
            ['name' => 'picker', 'guard_name' => 'web', 'display_name' => '拣货员', 'description' => '拣货任务、称重改价', 'created_at' => now(), 'updated_at' => now()],
            ['name' => 'driver', 'guard_name' => 'web', 'display_name' => '配送司机', 'description' => '配送任务、轨迹、签收', 'created_at' => now(), 'updated_at' => now()],
            ['name' => 'merchant', 'guard_name' => 'web', 'display_name' => '商家', 'description' => '小程序商家端', 'created_at' => now(), 'updated_at' => now()],
        ]);

        // 初始化超级管理员
        $userId = DB::table('users')->insertGetId([
            'username' => 'seeding',
            'password' => Hash::make('Password'),
            'name' => '系统管理员',
            'phone' => '15690631151',
            'email' => 'seeding@ihopeso.cn',
            'status' => 1,
            'created_at' => now(),
            'updated_at' => now(),
        ]);

        DB::table('model_has_roles')->insert([
            'role_id' => $roleIds,
            'model_type' => 'App\\Models\\User',
            'model_id' => $userId,
        ]);
    }

    public function down(): void
    {
        Schema::dropIfExists('personal_access_tokens');
        Schema::dropIfExists('password_reset_tokens');
        Schema::dropIfExists('role_has_permissions');
        Schema::dropIfExists('model_has_permissions');
        Schema::dropIfExists('model_has_roles');
        Schema::dropIfExists('permissions');
        Schema::dropIfExists('roles');
        Schema::dropIfExists('users');
    }
};
```

### 7.2 组织主体模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 供应商表
        Schema::create('suppliers', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('name', 100)->comment('供应商名称');
            $table->string('contact_name', 50)->nullable()->comment('联系人');
            $table->string('contact_phone', 20)->nullable()->comment('联系电话');
            $table->string('address', 255)->nullable()->comment('地址');
            $table->string('bank_name', 100)->nullable()->comment('开户行');
            $table->string('bank_account', 50)->nullable()->comment('银行账号');
            $table->tinyInteger('settlement_cycle')->unsigned()->default(1)->comment('结算周期：1周结，2月结');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0禁用，1启用');
            $table->text('remark')->nullable()->comment('备注');
            $table->timestamps();
            $table->softDeletes();

            $table->index('status');
            $table->comment('供应商表');
        });

        // 配送线路表
        Schema::create('delivery_routes', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('name', 50)->comment('线路名称');
            $table->string('description', 255)->nullable()->comment('描述');
            $table->unsignedInteger('sort')->default(0)->comment('排序');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态');
            $table->timestamps();
            $table->softDeletes();

            $table->index('status');
            $table->comment('配送线路表');
        });

        // 商家表
        Schema::create('merchants', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('user_id')->nullable()->comment('关联登录用户ID');
            $table->string('name', 100)->comment('商家名称');
            $table->string('contact_name', 50)->nullable()->comment('联系人');
            $table->string('contact_phone', 20)->nullable()->comment('联系电话');
            $table->string('address', 255)->nullable()->comment('默认配送地址');
            $table->unsignedBigInteger('delivery_route_id')->nullable()->comment('所属配送线路ID');
            $table->unsignedInteger('delivery_sort')->default(0)->comment('配送顺序');
            $table->decimal('min_order_amount', 15, 2)->default(0)->comment('起送价');
            $table->tinyInteger('settlement_type')->unsigned()->default(1)->comment('结算方式：1现结，2账期，3预付款');
            $table->decimal('credit_limit', 15, 2)->default(0)->comment('信用额度');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0禁用，1启用');
            $table->text('remark')->nullable()->comment('备注');
            $table->timestamps();
            $table->softDeletes();

            $table->index('user_id');
            $table->index('delivery_route_id');
            $table->index('status');
            $table->comment('商家表');
        });

        // 司机表
        Schema::create('drivers', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('user_id')->nullable()->comment('关联登录用户ID');
            $table->string('name', 50)->comment('姓名');
            $table->string('phone', 20)->unique()->comment('手机号');
            $table->string('id_card', 18)->nullable()->comment('身份证号');
            $table->tinyInteger('online_status')->unsigned()->default(0)->comment('在线状态：0离线，1在线');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0禁用，1启用');
            $table->timestamps();
            $table->softDeletes();

            $table->index('user_id');
            $table->index('status');
            $table->comment('司机表');
        });

        // 车辆表
        Schema::create('vehicles', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('plate_number', 20)->unique()->comment('车牌号');
            $table->string('vehicle_type', 50)->nullable()->comment('车辆类型');
            $table->tinyInteger('is_cold_chain')->unsigned()->default(0)->comment('是否冷链：0否，1是');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0禁用，1启用');
            $table->timestamps();
            $table->softDeletes();

            $table->index('status');
            $table->comment('车辆表');
        });

        // 司机车辆绑定表
        Schema::create('driver_vehicles', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('driver_id')->comment('司机ID');
            $table->unsignedBigInteger('vehicle_id')->comment('车辆ID');
            $table->tinyInteger('is_default')->unsigned()->default(0)->comment('是否默认车辆');
            $table->timestamp('bound_at')->nullable()->comment('绑定时间');
            $table->timestamp('unbound_at')->nullable()->comment('解绑时间');
            $table->timestamps();

            $table->index('driver_id');
            $table->index('vehicle_id');
            $table->comment('司机车辆绑定表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('driver_vehicles');
        Schema::dropIfExists('vehicles');
        Schema::dropIfExists('drivers');
        Schema::dropIfExists('merchants');
        Schema::dropIfExists('delivery_routes');
        Schema::dropIfExists('suppliers');
    }
};
```

### 7.3 商品管理模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 商品分类表
        Schema::create('categories', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('parent_id')->default(0)->comment('父级分类ID，0为根节点');
            $table->string('name', 50)->comment('分类名称');
            $table->string('icon', 255)->nullable()->comment('图标');
            $table->unsignedInteger('sort')->default(0)->comment('排序');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0禁用，1启用');
            $table->timestamps();
            $table->softDeletes();

            $table->index('parent_id');
            $table->index('status');
            $table->comment('商品分类表');
        });

        // 商品表
        Schema::create('products', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('category_id')->comment('分类ID');
            $table->unsignedBigInteger('supplier_id')->nullable()->comment('默认供应商ID');
            $table->string('name', 100)->comment('商品名称');
            $table->string('cover', 255)->nullable()->comment('封面图');
            $table->string('unit', 20)->comment('单位：斤/箱/份等');
            $table->tinyInteger('is_weight_priced')->unsigned()->default(0)->comment('是否称重改价：0否，1是');
            $table->decimal('stock_warning_value', 15, 3)->default(0)->comment('库存预警值');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0下架，1上架');
            $table->text('description')->nullable()->comment('商品详情');
            $table->timestamps();
            $table->softDeletes();

            $table->index('category_id');
            $table->index('supplier_id');
            $table->index('status');
            $table->comment('商品表');
        });

        // 商品图片表
        Schema::create('product_images', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('product_id')->comment('商品ID');
            $table->string('image_url', 255)->comment('图片地址');
            $table->unsignedInteger('sort')->default(0)->comment('排序');
            $table->timestamps();

            $table->index('product_id');
            $table->comment('商品图片表');
        });

        // SKU规格表
        Schema::create('skus', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('product_id')->comment('商品ID');
            $table->string('sku_code', 50)->unique()->comment('SKU编码');
            $table->json('specs')->nullable()->comment('规格属性');
            $table->decimal('purchase_price', 15, 4)->default(0)->comment('采购参考价');
            $table->decimal('wholesale_price', 15, 4)->default(0)->comment('批发销售价');
            $table->decimal('cost_price', 15, 4)->default(0)->comment('财务成本价');
            $table->decimal('stock', 15, 3)->default(0)->comment('当前库存冗余字段');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0禁用，1启用');
            $table->timestamps();
            $table->softDeletes();

            $table->index('product_id');
            $table->index('status');
            $table->comment('SKU规格表');
        });

        // 商家SKU可见性表
        Schema::create('merchant_sku_visibility', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('merchant_id')->comment('商家ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->tinyInteger('is_visible')->unsigned()->default(1)->comment('是否可见：0否，1是');
            $table->timestamps();

            $table->unique(['merchant_id', 'sku_id']);
            $table->index('sku_id');
            $table->comment('商家SKU可见性表');
        });

        // 标签词库表
        Schema::create('tags', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('name', 50)->unique()->comment('标签名称');
            $table->unsignedInteger('sort')->default(0)->comment('排序');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态');
            $table->timestamps();

            $table->comment('标签词库表');
        });

        // 商品标签关联表
        Schema::create('product_tags', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('product_id')->comment('商品ID');
            $table->unsignedBigInteger('tag_id')->comment('标签ID');
            $table->timestamps();

            $table->unique(['product_id', 'tag_id']);
            $table->index('tag_id');
            $table->comment('商品标签关联表');
        });

        // 搜索关键词表
        Schema::create('keywords', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('keyword', 50)->comment('关键词');
            $table->unsignedBigInteger('product_id')->nullable()->comment('关联商品ID');
            $table->unsignedInteger('search_count')->default(0)->comment('搜索次数');
            $table->timestamps();

            $table->index('product_id');
            $table->index('keyword');
            $table->comment('搜索关键词表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('keywords');
        Schema::dropIfExists('product_tags');
        Schema::dropIfExists('tags');
        Schema::dropIfExists('merchant_sku_visibility');
        Schema::dropIfExists('skus');
        Schema::dropIfExists('product_images');
        Schema::dropIfExists('products');
        Schema::dropIfExists('categories');
    }
};
```

### 7.4 平台统采模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 待采清单表
        Schema::create('purchase_items', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->decimal('quantity', 15, 3)->default(0)->comment('待采数量');
            $table->tinyInteger('source_type')->unsigned()->default(1)->comment('来源：1订单汇总，2手工添加');
            $table->unsignedBigInteger('source_id')->nullable()->comment('来源业务ID');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待生成采购单，2已生成采购单');
            $table->timestamps();

            $table->index('sku_id');
            $table->index('status');
            $table->comment('待采清单表');
        });

        // 采购单表
        Schema::create('purchase_orders', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('order_no', 50)->unique()->comment('采购单号');
            $table->unsignedBigInteger('supplier_id')->comment('供应商ID');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待接单，2备货中，3已发货，4已入库，5完成，9取消');
            $table->decimal('total_amount', 15, 2)->default(0)->comment('总金额');
            $table->decimal('actual_amount', 15, 2)->default(0)->comment('实际入库金额');
            $table->text('remark')->nullable()->comment('备注');
            $table->timestamps();
            $table->softDeletes();

            $table->index('supplier_id');
            $table->index('status');
            $table->comment('采购单表');
        });

        // 采购单明细表
        Schema::create('purchase_order_items', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('purchase_order_id')->comment('采购单ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->decimal('quantity', 15, 3)->default(0)->comment('采购数量');
            $table->decimal('price', 15, 4)->default(0)->comment('采购单价');
            $table->decimal('actual_quantity', 15, 3)->default(0)->comment('实际入库数量');
            $table->decimal('actual_price', 15, 4)->default(0)->comment('实际入库单价');
            $table->decimal('amount', 15, 2)->default(0)->comment('金额');
            $table->decimal('actual_amount', 15, 2)->default(0)->comment('实际金额');
            $table->string('discrepancy_reason', 255)->nullable()->comment('入库差异原因');
            $table->timestamps();

            $table->index('purchase_order_id');
            $table->index('sku_id');
            $table->comment('采购单明细表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('purchase_order_items');
        Schema::dropIfExists('purchase_orders');
        Schema::dropIfExists('purchase_items');
    }
};
```



### 7.5 客户直采模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 购物车表
        Schema::create('carts', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('merchant_id')->unique()->comment('商家ID');
            $table->timestamps();

            $table->comment('购物车表');
        });

        // 购物车明细表
        Schema::create('cart_items', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('cart_id')->comment('购物车ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->decimal('quantity', 15, 3)->default(0)->comment('数量');
            $table->decimal('price', 15, 4)->default(0)->comment('加入时单价');
            $table->timestamps();

            $table->index('cart_id');
            $table->index('sku_id');
            $table->comment('购物车明细表');
        });

        // 订单表
        Schema::create('orders', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('order_no', 50)->unique()->comment('订单号');
            $table->unsignedBigInteger('merchant_id')->comment('商家ID');
            $table->unsignedBigInteger('delivery_route_id')->nullable()->comment('配送线路ID');
            $table->tinyInteger('batch')->unsigned()->default(1)->comment('配送批次：1上午，2下午');
            $table->string('delivery_address', 255)->nullable()->comment('配送地址');
            $table->string('contact_name', 50)->nullable()->comment('收货联系人');
            $table->string('contact_phone', 20)->nullable()->comment('收货电话');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待拣货，2拣货中，3配送中，4已签收，5已锁定，9已取消');
            $table->decimal('total_amount', 15, 2)->default(0)->comment('原始订单金额');
            $table->decimal('adjusted_amount', 15, 2)->default(0)->comment('调整后金额');
            $table->decimal('final_amount', 15, 2)->default(0)->comment('最终结算金额');
            $table->tinyInteger('payment_status')->unsigned()->default(1)->comment('支付状态：1未支付，2已支付，3账期');
            $table->tinyInteger('settlement_type')->unsigned()->default(1)->comment('结算方式：1现结，2账期，3预付款');
            $table->tinyInteger('is_locked')->unsigned()->default(0)->comment('是否锁定：0否，1是');
            $table->text('remark')->nullable()->comment('备注');
            $table->timestamps();
            $table->softDeletes();

            $table->index('merchant_id');
            $table->index('delivery_route_id');
            $table->index('status');
            $table->index('batch');
            $table->comment('订单表');
        });

        // 订单明细表
        Schema::create('order_items', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('order_id')->comment('订单ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->string('product_name', 100)->comment('商品名称快照');
            $table->json('sku_specs')->nullable()->comment('规格快照');
            $table->decimal('quantity', 15, 3)->default(0)->comment('下单数量');
            $table->decimal('price', 15, 4)->default(0)->comment('下单单价');
            $table->decimal('actual_quantity', 15, 3)->default(0)->comment('实际称重数量');
            $table->decimal('actual_price', 15, 4)->default(0)->comment('实际称重单价');
            $table->decimal('subtotal', 15, 2)->default(0)->comment('小计金额');
            $table->decimal('actual_subtotal', 15, 2)->default(0)->comment('实际小计金额');
            $table->decimal('discrepancy_amount', 15, 2)->default(0)->comment('差异金额');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1正常，2待审核，3已调整');
            $table->timestamps();

            $table->index('order_id');
            $table->index('sku_id');
            $table->comment('订单明细表');
        });

        // 常购清单表
        Schema::create('frequently_bought', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('merchant_id')->comment('商家ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->unsignedInteger('buy_count')->default(0)->comment('购买次数');
            $table->timestamp('last_buy_at')->nullable()->comment('最近购买时间');
            $table->timestamps();

            $table->unique(['merchant_id', 'sku_id']);
            $table->index('sku_id');
            $table->comment('常购清单表');
        });

        // 复购模板表
        Schema::create('repurchase_templates', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('merchant_id')->comment('商家ID');
            $table->string('name', 50)->comment('模板名称');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态');
            $table->timestamps();

            $table->index('merchant_id');
            $table->comment('复购模板表');
        });

        // 复购模板明细表
        Schema::create('repurchase_template_items', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('template_id')->comment('模板ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->decimal('quantity', 15, 3)->default(0)->comment('数量');
            $table->timestamps();

            $table->index('template_id');
            $table->index('sku_id');
            $table->comment('复购模板明细表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('repurchase_template_items');
        Schema::dropIfExists('repurchase_templates');
        Schema::dropIfExists('frequently_bought');
        Schema::dropIfExists('order_items');
        Schema::dropIfExists('orders');
        Schema::dropIfExists('cart_items');
        Schema::dropIfExists('carts');
    }
};
```

### 7.6 库存管理模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 仓库表
        Schema::create('warehouses', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('name', 50)->comment('仓库名称');
            $table->tinyInteger('type')->unsigned()->default(1)->comment('类型：1总仓，2前置仓');
            $table->tinyInteger('is_cold_chain')->unsigned()->default(0)->comment('是否冷链：0否，1是');
            $table->string('address', 255)->nullable()->comment('地址');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态');
            $table->timestamps();
            $table->softDeletes();

            $table->index('status');
            $table->comment('仓库表');
        });

        // 实时库存表
        Schema::create('inventory', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('warehouse_id')->comment('仓库ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->decimal('total_stock', 15, 3)->default(0)->comment('总库存');
            $table->decimal('locked_stock', 15, 3)->default(0)->comment('锁定库存');
            $table->decimal('available_stock', 15, 3)->default(0)->comment('可用库存');
            $table->string('batch_no', 50)->nullable()->comment('入库批次号');
            $table->date('expiry_date')->nullable()->comment('效期');
            $table->decimal('warning_value', 15, 3)->default(0)->comment('预警值');
            $table->timestamps();

            $table->unique(['warehouse_id', 'sku_id', 'batch_no']);
            $table->index('sku_id');
            $table->comment('实时库存表');
        });

        // 库存变动日志表
        Schema::create('inventory_logs', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('warehouse_id')->comment('仓库ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->tinyInteger('type')->unsigned()->comment('变动类型：1入库，2出库，3调拨，4报损，5报溢，6调整');
            $table->decimal('quantity', 15, 3)->default(0)->comment('变动数量，正增负减');
            $table->decimal('before_stock', 15, 3)->default(0)->comment('变动前库存');
            $table->decimal('after_stock', 15, 3)->default(0)->comment('变动后库存');
            $table->string('reason', 255)->nullable()->comment('变动原因');
            $table->unsignedBigInteger('operator_id')->nullable()->comment('操作人ID');
            $table->string('source_type', 50)->nullable()->comment('业务来源类型');
            $table->unsignedBigInteger('source_id')->nullable()->comment('业务来源ID');
            $table->timestamp('created_at')->nullable()->comment('创建时间');

            $table->index('warehouse_id');
            $table->index('sku_id');
            $table->index('type');
            $table->index('created_at');
            $table->comment('库存变动日志表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('inventory_logs');
        Schema::dropIfExists('inventory');
        Schema::dropIfExists('warehouses');
    }
};
```

### 7.7 拣货管理模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 拣货任务表
        Schema::create('picking_tasks', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('task_no', 50)->unique()->comment('任务编号');
            $table->unsignedBigInteger('warehouse_id')->comment('仓库ID');
            $table->unsignedBigInteger('picker_id')->nullable()->comment('拣货员ID');
            $table->tinyInteger('batch')->unsigned()->default(1)->comment('配送批次：1上午，2下午');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待分配，2拣货中，3已完成');
            $table->timestamp('started_at')->nullable()->comment('开始时间');
            $table->timestamp('completed_at')->nullable()->comment('完成时间');
            $table->timestamps();

            $table->index('warehouse_id');
            $table->index('picker_id');
            $table->index('status');
            $table->comment('拣货任务表');
        });

        // 拣货任务明细表
        Schema::create('picking_task_items', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('picking_task_id')->comment('拣货任务ID');
            $table->unsignedBigInteger('order_id')->comment('订单ID');
            $table->unsignedBigInteger('order_item_id')->comment('订单明细ID');
            $table->unsignedBigInteger('sku_id')->comment('SKU ID');
            $table->decimal('required_quantity', 15, 3)->default(0)->comment('需求数量');
            $table->decimal('picked_quantity', 15, 3)->default(0)->comment('实际拣货数量');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待拣货，2已拣货，3差异');
            $table->timestamps();

            $table->index('picking_task_id');
            $table->index('order_id');
            $table->index('sku_id');
            $table->comment('拣货任务明细表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('picking_task_items');
        Schema::dropIfExists('picking_tasks');
    }
};
```

### 7.8 物流配送模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 配送任务表
        Schema::create('delivery_tasks', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('task_no', 50)->unique()->comment('任务编号');
            $table->unsignedBigInteger('delivery_route_id')->comment('线路ID');
            $table->unsignedBigInteger('driver_id')->nullable()->comment('司机ID');
            $table->unsignedBigInteger('vehicle_id')->nullable()->comment('车辆ID');
            $table->tinyInteger('batch')->unsigned()->default(1)->comment('配送批次：1上午，2下午');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待配送，2配送中，3任务完成');
            $table->timestamp('planned_at')->nullable()->comment('计划配送时间');
            $table->timestamp('started_at')->nullable()->comment('开始时间');
            $table->timestamp('completed_at')->nullable()->comment('完成时间');
            $table->timestamps();

            $table->index('delivery_route_id');
            $table->index('driver_id');
            $table->index('status');
            $table->comment('配送任务表');
        });

        // 配送任务订单关联表
        Schema::create('delivery_task_orders', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('delivery_task_id')->comment('配送任务ID');
            $table->unsignedBigInteger('order_id')->comment('订单ID');
            $table->unsignedInteger('delivery_sort')->default(0)->comment('配送顺序');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待配送，2已送达');
            $table->timestamps();

            $table->index('delivery_task_id');
            $table->index('order_id');
            $table->comment('配送任务订单关联表');
        });

        // 配送轨迹表
        Schema::create('delivery_tracks', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('delivery_task_id')->comment('配送任务ID');
            $table->unsignedBigInteger('driver_id')->comment('司机ID');
            $table->decimal('latitude', 10, 7)->default(0)->comment('纬度');
            $table->decimal('longitude', 10, 7)->default(0)->comment('经度');
            $table->string('location_desc', 255)->nullable()->comment('位置描述');
            $table->timestamp('reported_at')->nullable()->comment('上报时间');
            $table->timestamp('created_at')->nullable()->comment('创建时间');

            $table->index('delivery_task_id');
            $table->index('driver_id');
            $table->index('reported_at');
            $table->comment('配送轨迹表');
        });

        // 签收存证表
        Schema::create('signatures', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('order_id')->comment('订单ID');
            $table->unsignedBigInteger('delivery_task_id')->comment('配送任务ID');
            $table->tinyInteger('type')->unsigned()->default(1)->comment('类型：1拍照签收，2电子签名，3质检照片');
            $table->string('image_url', 255)->nullable()->comment('图片/签名文件地址');
            $table->string('signer_name', 50)->nullable()->comment('签收人');
            $table->timestamp('signed_at')->nullable()->comment('签收时间');
            $table->timestamps();

            $table->index('order_id');
            $table->index('delivery_task_id');
            $table->index('type');
            $table->comment('签收存证表');
        });

        // 冷链温度记录表
        Schema::create('temperatures', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('delivery_task_id')->comment('配送任务ID');
            $table->decimal('temperature', 5, 2)->default(0)->comment('温度值');
            $table->timestamp('recorded_at')->nullable()->comment('记录时间');
            $table->timestamp('created_at')->nullable()->comment('创建时间');

            $table->index('delivery_task_id');
            $table->index('recorded_at');
            $table->comment('冷链温度记录表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('temperatures');
        Schema::dropIfExists('signatures');
        Schema::dropIfExists('delivery_tracks');
        Schema::dropIfExists('delivery_task_orders');
        Schema::dropIfExists('delivery_tasks');
    }
};
```

### 7.9 差异处理模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 差异单表
        Schema::create('discrepancies', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('discrepancy_no', 50)->unique()->comment('差异单号');
            $table->unsignedBigInteger('order_id')->comment('关联订单ID');
            $table->unsignedBigInteger('order_item_id')->nullable()->comment('关联订单明细ID');
            $table->tinyInteger('stage')->unsigned()->comment('差异环节：1拣货，2配送，3实收');
            $table->tinyInteger('type')->unsigned()->default(1)->comment('差异类型：1少收，2拒收，3残次，4其他');
            $table->decimal('expected_quantity', 15, 3)->default(0)->comment('预期数量');
            $table->decimal('actual_quantity', 15, 3)->default(0)->comment('实际数量');
            $table->decimal('quantity_diff', 15, 3)->default(0)->comment('数量差异');
            $table->decimal('amount_diff', 15, 2)->default(0)->comment('金额差异');
            $table->string('reason', 255)->nullable()->comment('差异原因');
            $table->json('evidence_urls')->nullable()->comment('凭证图片数组');
            $table->tinyInteger('responsible_party')->unsigned()->nullable()->comment('责任方：1供应商，2平台，3司机，4商家');
            $table->tinyInteger('decision')->unsigned()->nullable()->comment('处理决策：1补货，2退款，3扣款，4报损，5不计');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待处理，2处理中，3已处理，4已关闭');
            $table->unsignedBigInteger('handler_id')->nullable()->comment('处理人ID');
            $table->timestamp('handled_at')->nullable()->comment('处理时间');
            $table->tinyInteger('is_amount_adjusted')->unsigned()->default(0)->comment('是否已调整金额');
            $table->text('remark')->nullable()->comment('备注');
            $table->timestamps();
            $table->softDeletes();

            $table->index('order_id');
            $table->index('order_item_id');
            $table->index('stage');
            $table->index('status');
            $table->comment('差异单表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('discrepancies');
    }
};
```

### 7.10 财务对账模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 商家账户表
        Schema::create('merchant_accounts', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('merchant_id')->unique()->comment('商家ID');
            $table->decimal('balance', 15, 2)->default(0)->comment('账户余额');
            $table->decimal('total_recharge', 15, 2)->default(0)->comment('总充值');
            $table->decimal('total_consumption', 15, 2)->default(0)->comment('总消费');
            $table->decimal('credit_limit', 15, 2)->default(0)->comment('信用额度');
            $table->timestamps();

            $table->comment('商家账户表');
        });

        // 充值记录表
        Schema::create('recharges', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('merchant_id')->comment('商家ID');
            $table->decimal('amount', 15, 2)->default(0)->comment('充值金额');
            $table->tinyInteger('payment_method')->unsigned()->default(1)->comment('支付方式：1微信支付，2线下转账，3后台手工');
            $table->string('transaction_no', 100)->nullable()->comment('第三方交易号');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待确认，2成功，3失败');
            $table->unsignedBigInteger('operator_id')->nullable()->comment('操作人ID');
            $table->string('remark', 255)->nullable()->comment('备注');
            $table->timestamps();

            $table->index('merchant_id');
            $table->index('transaction_no');
            $table->index('status');
            $table->comment('充值记录表');
        });

        // 供应商结算单表
        Schema::create('supplier_settlements', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('settlement_no', 50)->unique()->comment('结算单号');
            $table->unsignedBigInteger('supplier_id')->comment('供应商ID');
            $table->date('start_date')->comment('结算周期开始');
            $table->date('end_date')->comment('结算周期结束');
            $table->decimal('total_amount', 15, 2)->default(0)->comment('汇总金额');
            $table->decimal('service_fee', 15, 2)->default(0)->comment('服务费');
            $table->decimal('payable_amount', 15, 2)->default(0)->comment('应付金额');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待结算，2已结算');
            $table->timestamp('settled_at')->nullable()->comment('结算时间');
            $table->timestamps();

            $table->index('supplier_id');
            $table->index('status');
            $table->comment('供应商结算单表');
        });

        // 结算单明细表
        Schema::create('supplier_settlement_items', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('supplier_settlement_id')->comment('结算单ID');
            $table->unsignedBigInteger('purchase_order_id')->comment('采购单ID');
            $table->decimal('amount', 15, 2)->default(0)->comment('金额');
            $table->timestamps();

            $table->index('supplier_settlement_id');
            $table->index('purchase_order_id');
            $table->comment('结算单明细表');
        });

        // 应收账款表
        Schema::create('receivables', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('receivable_no', 50)->unique()->comment('应收单号');
            $table->unsignedBigInteger('order_id')->comment('订单ID');
            $table->unsignedBigInteger('merchant_id')->comment('商家ID');
            $table->decimal('original_amount', 15, 2)->default(0)->comment('原始金额');
            $table->decimal('adjusted_amount', 15, 2)->default(0)->comment('调整后金额');
            $table->decimal('discrepancy_amount', 15, 2)->default(0)->comment('差异金额');
            $table->decimal('paid_amount', 15, 2)->default(0)->comment('已付金额');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1未结算，2已结算，3争议中');
            $table->tinyInteger('settlement_type')->unsigned()->default(1)->comment('结算方式：1现结，2账期，3预付款');
            $table->date('due_date')->nullable()->comment('到期日');
            $table->timestamp('settled_at')->nullable()->comment('结算时间');
            $table->timestamps();

            $table->index('order_id');
            $table->index('merchant_id');
            $table->index('status');
            $table->comment('应收账款表');
        });

        // 发票表
        Schema::create('invoices', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('invoice_no', 50)->nullable()->comment('发票号');
            $table->tinyInteger('type')->unsigned()->default(1)->comment('类型：1客户发票，2供应商发票');
            $table->unsignedBigInteger('target_id')->comment('关联对象ID');
            $table->string('title', 100)->nullable()->comment('发票抬头');
            $table->decimal('amount', 15, 2)->default(0)->comment('金额');
            $table->string('file_url', 255)->nullable()->comment('发票文件地址');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：1待开具，2已开具，3已寄出');
            $table->timestamp('applied_at')->nullable()->comment('申请时间');
            $table->timestamp('issued_at')->nullable()->comment('开具时间');
            $table->timestamp('sent_at')->nullable()->comment('寄出时间');
            $table->timestamps();

            $table->index('target_id');
            $table->index('type');
            $table->index('status');
            $table->comment('发票表');
        });

        // 单据授权更正表
        Schema::create('correction_authorizations', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('order_id')->comment('订单ID');
            $table->unsignedBigInteger('operator_id')->comment('授权人ID');
            $table->string('reason', 255)->comment('更正原因');
            $table->json('before_data')->nullable()->comment('修改前数据');
            $table->json('after_data')->nullable()->comment('修改后数据');
            $table->timestamp('authorized_at')->nullable()->comment('授权时间');
            $table->timestamps();

            $table->index('order_id');
            $table->index('operator_id');
            $table->comment('单据授权更正表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('correction_authorizations');
        Schema::dropIfExists('invoices');
        Schema::dropIfExists('receivables');
        Schema::dropIfExists('supplier_settlement_items');
        Schema::dropIfExists('supplier_settlements');
        Schema::dropIfExists('recharges');
        Schema::dropIfExists('merchant_accounts');
    }
};
```

### 7.11 系统支撑模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        // 系统配置表
        Schema::create('system_configs', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('config_key', 100)->unique()->comment('配置键');
            $table->text('config_value')->nullable()->comment('配置值');
            $table->string('description', 255)->nullable()->comment('说明');
            $table->timestamps();

            $table->comment('系统配置表');
        });

        // 轮播广告表
        Schema::create('banners', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('title', 100)->comment('标题');
            $table->string('image_url', 255)->comment('图片地址');
            $table->string('link_url', 255)->nullable()->comment('跳转链接');
            $table->unsignedInteger('sort')->default(0)->comment('排序');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态：0禁用，1启用');
            $table->timestamps();
            $table->softDeletes();

            $table->index('status');
            $table->index('sort');
            $table->comment('轮播广告表');
        });

        // 运营主推表
        Schema::create('promotions', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->tinyInteger('type')->unsigned()->default(1)->comment('类型：1主推商品，2主推品类');
            $table->unsignedBigInteger('target_id')->comment('目标ID');
            $table->unsignedInteger('sort')->default(0)->comment('排序');
            $table->timestamp('start_at')->nullable()->comment('开始时间');
            $table->timestamp('end_at')->nullable()->comment('结束时间');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态');
            $table->timestamps();

            $table->index('type');
            $table->index('target_id');
            $table->index('status');
            $table->comment('运营主推表');
        });

        // 操作日志表
        Schema::create('operation_logs', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('user_id')->nullable()->comment('操作人ID');
            $table->string('username', 50)->nullable()->comment('操作人用户名');
            $table->string('method', 10)->nullable()->comment('请求方法');
            $table->string('path', 255)->nullable()->comment('请求路径');
            $table->string('ip', 50)->nullable()->comment('IP地址');
            $table->text('content')->nullable()->comment('操作内容');
            $table->timestamp('created_at')->nullable()->comment('创建时间');

            $table->index('user_id');
            $table->index('created_at');
            $table->comment('操作日志表');
        });

        // 审计日志表
        Schema::create('audit_logs', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->string('model_type', 100)->comment('模型类型');
            $table->unsignedBigInteger('model_id')->comment('模型ID');
            $table->string('action', 50)->comment('操作动作');
            $table->json('before_data')->nullable()->comment('修改前数据');
            $table->json('after_data')->nullable()->comment('修改后数据');
            $table->unsignedBigInteger('operator_id')->nullable()->comment('操作人ID');
            $table->string('reason', 255)->nullable()->comment('操作原因');
            $table->string('relation_type', 50)->nullable()->comment('关联类型');
            $table->unsignedBigInteger('relation_id')->nullable()->comment('关联ID');
            $table->timestamp('created_at')->nullable()->comment('创建时间');

            $table->index(['model_type', 'model_id']);
            $table->index('operator_id');
            $table->index('created_at');
            $table->comment('审计日志表');
        });

        // 默认系统配置
        DB::table('system_configs')->insert([
            ['config_key' => 'site_name', 'config_value' => '本地速送服务平台', 'description' => '站点名称', 'created_at' => now(), 'updated_at' => now()],
            ['config_key' => 'contact_phone', 'config_value' => '15690631151', 'description' => '客服电话', 'created_at' => now(), 'updated_at' => now()],
            ['config_key' => 'default_delivery_batch', 'config_value' => '1', 'description' => '默认配送批次：1上午，2下午', 'created_at' => now(), 'updated_at' => now()],
            ['config_key' => 'weighing_diff_threshold', 'config_value' => '20', 'description' => '称重差异阈值（百分比）', 'created_at' => now(), 'updated_at' => now()],
        ]);
    }

    public function down(): void
    {
        Schema::dropIfExists('audit_logs');
        Schema::dropIfExists('operation_logs');
        Schema::dropIfExists('promotions');
        Schema::dropIfExists('banners');
        Schema::dropIfExists('system_configs');
    }
};
```

### 7.12 微信模块

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // 微信用户绑定表
        Schema::create('wechat_users', function (Blueprint $table) {
            $table->id()->comment('主键');
            $table->unsignedBigInteger('user_id')->nullable()->comment('关联系统用户ID');
            $table->string('openid', 100)->unique()->comment('微信OpenID');
            $table->string('unionid', 100)->nullable()->comment('微信UnionID');
            $table->string('nickname', 50)->nullable()->comment('昵称');
            $table->string('avatar', 255)->nullable()->comment('头像');
            $table->tinyInteger('type')->unsigned()->default(1)->comment('类型：1商家端，2司机端');
            $table->tinyInteger('status')->unsigned()->default(1)->comment('状态');
            $table->timestamps();

            $table->index('user_id');
            $table->index('type');
            $table->comment('微信用户绑定表');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('wechat_users');
    }
};
```

---

## 8 初始化数据说明

执行上述 Migration 后，将自动填充以下默认数据：

| 数据类型 | 内容 | 说明 |
| :--- | :--- | :--- |
| 角色 | 超级管理员、运营管理员、财务人员、拣货员、配送司机、商家 | 基于 PRD 角色定义 |
| 管理员账号 | `seeding` / `seeding@ihopeso.cn` / `15690631151` | 初始密码：`Password`（bcrypt 加密） |
| 系统配置 | 站点名称、客服电话、默认配送批次、称重差异阈值 | 业务初始默认值 |

> 生产环境部署前，请修改默认管理员密码。

---

## 9 附：字段类型速查

| 业务含义 | 推荐类型 | 示例 |
| :--- | :--- | :--- |
| 主键 | `bigint unsigned` 自增 | `id` |
| 状态枚举 | `tinyint unsigned` | `status` |
| 金额 | `decimal(15, 2)` 或 `decimal(15, 4)` | `total_amount`, `price` |
| 数量/重量 | `decimal(15, 3)` | `quantity`, `stock` |
| 坐标 | `decimal(10, 7)` | `latitude`, `longitude` |
| 快照 JSON | `json` | `sku_specs`, `before_data` |
| 业务编号 | `varchar(50)` 唯一 | `order_no`, `task_no` |
| 时间 | `timestamp` | `created_at`, `updated_at` |

---

## 10 关联 attach 文件

- `attach/init.sql`：完整数据库初始化 SQL（含建表、索引、默认数据）
- `attach/DB数据字典.xlsx`：Excel 版本数据字典
- `attach/.env.example`：数据库连接环境变量模板
- `attach/install.bat` / `attach/install.sh`：一键安装脚本（含数据库初始化）

---

**编制日期**：2026-07-25  
**版本**：V1.0
